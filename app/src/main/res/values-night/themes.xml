<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TransactionApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- General -->
        <item name="colorPrimary">@color/N9</item>
        <item name="colorSecondary">@color/N1</item>
        <item name="colorAccent">@color/G5</item>

        <!-- App -->
        <item name="android:windowBackground">?attr/colorPrimary</item>
    </style>
</resources>